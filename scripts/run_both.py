import subprocess
r = subprocess.run(["curl", "-s",
    "https://telegram-referral-bot-gules.vercel.app/api/cron/payout-queue",
    "-H", "Authorization: Bearer *** subprocess.run(["curl", "-s",
    "https://telegram-referral-bot-gules.vercel.app/api/cron/sweep",
    "-H", "Authorization: Bearer *** print("Sweep:", r2.stdout)
